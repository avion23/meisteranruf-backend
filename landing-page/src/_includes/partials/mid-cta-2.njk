<style>
    .mid-cta-2 {
        background: var(--light-gray);
        padding: 3rem 0;
        text-align: center;
        border-top: 2px solid #e5e7eb;
        border-bottom: 2px solid #e5e7eb;
    }
    .mid-cta-2 h2 {
        color: var(--primary);
        font-size: 1.7rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }
    .mid-cta-2 p {
        color: var(--dark-gray);
        font-size: 1.05rem;
        margin-bottom: 2rem;
        max-width: 650px;
        margin-left: auto;
        margin-right: auto;
    }
    .mid-cta-2 .cta-button {
        display: inline-block;
        background: var(--accent);
        color: var(--white);
        padding: 1rem 2.5rem;
        text-decoration: none;
        font-weight: 700;
        font-size: 1.15rem;
        border-radius: 4px;
        transition: all 0.2s ease;
        box-shadow: 0 4px 14px rgba(249, 115, 22, 0.3);
        border: 2px solid var(--accent);
    }
    .mid-cta-2 .cta-button:hover {
        background: var(--accent-darker);
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(249, 115, 22, 0.4);
    }
    .mid-cta-2 .sub-cta {
        display: block;
        margin-top: 1rem;
        color: var(--primary);
        font-weight: 600;
        font-size: 0.95rem;
        text-decoration: none;
    }
    .mid-cta-2 .sub-cta:hover {
        text-decoration: underline;
    }
    .mid-cta-2 .urgency-box {
        background: #fffbeb;
        border-left: 4px solid var(--accent);
        padding: 1rem 1.5rem;
        margin: 2rem auto 0;
        max-width: 500px;
        border-radius: 4px;
    }
    .mid-cta-2 .urgency-box p {
        font-size: 0.9rem;
        color: var(--dark-gray);
        margin: 0;
        font-style: italic;
    }
    @media (max-width: 768px) {
        .mid-cta-2 h2 {
            font-size: 1.3rem;
        }
        .mid-cta-2 p {
            font-size: 0.95rem;
            padding: 0 1rem;
        }
        .mid-cta-2 .cta-button {
            padding: 0.9rem 2rem;
            font-size: 1rem;
        }
    }
</style>

<section class="mid-cta-2">
    <div class="container">
        <h2>Bereit durchzustarten?</h2>
        <p>Starte jetzt und erhalte deine ersten Anruf-Anfragen morgen. Keine Installation, keine Schulung notwendig.</p>
        <a href="#kontakt" class="cta-button">Jetzt starten</a>

        <a href="#preise" class="sub-cta">Preise anzeigen →</a>

        <div class="urgency-box">
            <p>
                <strong>💡 Tipp:</strong> Die meisten Installateure starten am Montag. Wenn du heute startest, kannst du diese Woche schon erste Anrufe erhalten.
            </p>
        </div>
    </div>
</section>
